# Copy to backend/.env and adjust. Loaded by server.mjs at boot.
PORT=8787

# Question generation: "mock" (offline, default) or "anthropic".
AI=mock

# Only needed when AI=anthropic. Never commit the real value.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
